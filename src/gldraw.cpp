#include "gl/gl3.h"
#include "gldraw.h"
/*
typedef struct{
    GLuint vao;         // for glBindVertexArray(mesh->vao);
    GLuint vbo0;        // for glBindBuffer(GL_ARRAY_BUFFER, mesh->vbo[0]);
    GLuint vbo1;        // for glBindBuffer(GL_ELEMENT_ARRAY_BUFFER, mesh->vbo[1]);
    int vertexcount;   // for index_type = GL_UNSIGNED_BYTE;
    int indexcount;     // for glDrawElements(GL_TRIANGLES, indexcount, index_type, (GLvoid*)0);
    GLfloat modelmatrix[16];    // ModelMatrix
    char color[4];      // Mesh colour
} tDrawItemVertexNormal;

tDrawItemVertexNormal *drawVertexNormal;
int drawVertexNormal_count = 0;

void GLDrawInit()
{
}

void GLDrawDone()
{
}

void AddDrawItem(GLuint vao, GLuint vbo0, GLuint vbo1, int vertexcount, int indexcount, GLfloat *modelmatrix, char *color)
{
    drawVertexNormal[drawVertexNormal_count].vao = vao;
    drawVertexNormal[drawVertexNormal_count].vbo0 = vbo0;
    drawVertexNormal[drawVertexNormal_count].vbo1 = vbo1;
    drawVertexNormal[drawVertexNormal_count].vertexcount = vertexcount;
    drawVertexNormal[drawVertexNormal_count].indexcount = indexcount;
    drawVertexNormal[drawVertexNormal_count].mode;
}
*/
